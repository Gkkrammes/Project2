<div class="head">

<div class="jumbotron">
  <h1 class="display-4">Party Time!</h1>
  <a href='/create' type="button" class="btn btn-primary btn-lg" id="newPartyButton">Create a Party</a>
  <hr>
  <p class="lead">Select a party below and enter the password you were given by the host.</p>
</div>
        <div class="row justify-content-center">
            <div class="card">
                <ul>
                    {{#each parties}}
                    
                    {{!-- <p>{{dataValues.name}} <button type="submit" class="select btn btn-primary" id="selectNewPartyButton"
                            data-id="{{dataValues.id}}">Select</button></p> --}}
                    <p><a href='rsvp/{{dataValues.id}}' class="select btn btn-primary" id="selectNewPartyButton">{{dataValues.name}}</a></p>
                    {{/each}}
                </ul>
            </div>
        </div>
    
